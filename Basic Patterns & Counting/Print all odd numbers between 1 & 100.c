#include<stdio.h>

int main(){

    for(int i = 1 ; i <= 100 ; i++){//a counter to count from 1 to 100
        //check if the number is odd if it is print it
		if(i % 2 != 0)
			printf("%i ",i);//print the value of the counter
    }
}
